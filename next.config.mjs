/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'opulent-hornet-678.convex.cloud',
      },
    ],
  },
}

export default nextConfig
